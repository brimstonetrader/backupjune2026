1.

Let $F_k : \{0, 1\}^n \leftarrow \{0, 1\}^n$. 

Consider the PRF $F'_\kappa : \{0, 1\}^{n-1} \leftarrow \{0, 1\}^{2n}$ where $F'_\kappa (x) = F(0||x) || F(0||x)$. An adversary can determine whether a message was generated by this scheme by checking whether the first half is equal to the second half, which is true of 100% of inputs under this construction, but only a proportion of 1/2^n of all possible inputs to a true random function.

Consider the PRF $F'_\kappa : \{0, 1\}^{n-1} \leftarrow \{0, 1\}^{2n}$ where $F'_\kappa (x) = F(1||x) || F(x||1)$. An adversary can query the oracle for $x = 1^{n-1}$, and return true if the first half is equal to the second half, and false otherwise. Again, this is true of 100% of inputs under this construction, but only a proportion of 1/2^n of all possible inputs to a true random function.

Consider the PRF $F'_\kappa : \{0, 1\}^{n-1} \leftarrow \{0, 1\}^{2n}$ where $F'_\kappa (x) = F(0||x) || F(0||\not x)$. An adversary can query the oracle for $x = 1^{n-1}$ and $x = 0^{n-1}$, and return true if the first halves are equal of both. Again, this is true of 100% of inputs under this construction, but only a proportion of 1/2^n of all possible inputs to a true random function.

2a. No, this encryption scheme is not secure. The attacker can xor the two halves of the message to find $F_\kappa (msg)$, regardless of $r$. This allows them to distinguish that chosen plaintext from another message with non-negligible probability.

b.

Suppose we have an encryption scheme where 

$Enc(\kappa, msg) := <r, F_\kappa (msg \ocross r)>$, for some random $r$.

$Dec(r, s) = (F_\kappa (msg)) \ocross r$

To prove that this scheme is secure, we can first suppose there exists a PPT adversary $A_{cpa}$ which can query an encryption oracle $q(n)$ times. 

i. The adversary can send some message $msg$.

ii. The PRF distinguisher generates a random string $r$, and compute $x = msg \ocross r$.

iii. This $x$ is given to the PRF challenger, who returns $F_\kappa(x)$ for some pseudorandom function $F_\kappa$.

iv. The PRF distinguisher gives $ct = <r, F_\kappa(x)>$. All of this can happen for some number of messages less than or equal to $q(n)$.

v. Next, the CPA adversary can give two messages $m_0, m_1$, of which they know the generated ciphertext for both.

vi. The PRF Distinguisher generates a random bit $b$, a random string $r'$, and gives the PRF challenger $x' = $

vii. The PRF Challenger computes $F_\kappa(x')$, and sends this to the Distinguisher.

viii. The Distinguisher sends $ct = <r', F_\kappa(x')>$ to the adversary. The adversary sends a bit corresponding to their guess of which message was chosen. If they are correct, the game ends with a 1-bit, and 0-bit otherwise.


$P(EXP^{IND-CPA}_{A_{cpa},S'}(n) = 1) = $

$P(EXP^{IND-CPA}_{A_{cpa},S'}(n) = 1 ^ repeated_r') + P(EXP^{IND-CPA}_{A_{cpa},S'}(n) = 1 ^ \not repeated_r') = $

$P(repeated_r') + \frac{1}{2} = $

$\frac{q(n)}{2^n} + \frac{1}{2}$

Thus $P(EXP^{IND-CPA}_{A_{cpa},S}(n) = 1) - P(EXP^{IND-CPA}_{A_{cpa},S'}(n) = 1) \leq \mathbf{negl}(n)$.


Thus $P(EXP^{IND-CPA}_{A_{cpa},S}(n) = 1) \leq \frac{q(n)}{2^n} + \frac{1}{2} + \mathbf{negl}(n) = \frac{1}{2} + \mathbf{negl}_1(n) + \mathbf{negl}_2(n)$. Because any PPT adversary cannot non-negligibly break $S'$, this scheme is CPA-secure.



3. No. The adversary could query an all-zero string, and receive $F_\kappa(0^n)||F_\kappa(r)$. They can then output the pair $(F_\kappa(r), F_\kappa(0^n))$, which will be accepted as the encoding of $r$. Thus, the adversary is able to forge a message without first querying it, and the system is forgeable.


4. 